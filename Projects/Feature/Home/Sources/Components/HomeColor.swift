import SwiftUI

enum HomeColor {
    /// Figma의 그룹 카드 배경색(#282726). DesignSystem의 gray900(#1D1C1C)과 다른 값이라 별도 정의한다.
    static let groupCardBackground = Color(red: 40 / 255, green: 39 / 255, blue: 38 / 255)
}
