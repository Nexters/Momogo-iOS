// xcconfig은 "//"를 주석 시작으로 처리하므로 $() 빈 매크로로 끊어서 이스케이프한다.
// 이 파일을 같은 폴더에 PROD.xcconfig로 복사하고 실제 값을 채운 뒤 사용한다 (PROD.xcconfig는 gitignore 대상).
// ⚠️ 이 .template 파일 자체를 수정해서 쓰지 말 것 — 복사본(PROD.xcconfig)에만 실값을 채운다.
API_BASE_URL = https:/$()/example.com/api/v1/
